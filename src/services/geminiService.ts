// This service is a placeholder - no external AI APIs are used
// All clinical redactions are generated internally from form data

import { FormDataState } from '@/types/historiaClinica';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const generateMedicalReport = async (_formData: FormDataState): Promise<string> => {
  // Return empty string - actual generation happens within form components
  return '';
};
