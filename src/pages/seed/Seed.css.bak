/* ════════════════════════════════════════════════════════
   DENTAXY SEED — PREMIUM WHITE CLINICAL DESIGN SYSTEM v2.0
   Tipografía: Plus Jakarta Sans + Inter + JetBrains Mono
   Paleta: Blanco puro · Verde vibrante · Tinta oscura
   ════════════════════════════════════════════════════════ */

@import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=Inter:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&family=Stalinist+One&family=Audiowide&family=Syncopate:wght@400;700&family=Orbitron:wght@400;500;700;900&display=swap');

/* ── TOKENS ── */
:root {
  /* Fondos */
  --s-bg:         #FFFFFF;
  --s-bg-alt:     #F7F8FA;
  --s-bg-card:    #FFFFFF;

  /* Tinta */
  --s-ink:        #09090F;
  --s-ink2:       #3A3A4A;
  --s-ink3:       #72728A;
  --s-ink4:       #A8A8BC;

  /* Verde Vibrante */
  --s-green:      #00C980;
  --s-green-d:    #00A668;
  --s-green-l:    rgba(0,201,128,0.10);
  --s-green-glow: rgba(0,201,128,0.22);
  --s-grad:       linear-gradient(135deg,#00C980 0%,#00E5A0 100%);

  /* Azul confianza */
  --s-blue:       #3B82F6;
  --s-blue-l:     rgba(59,130,246,0.10);

  /* Rojo error */
  --s-red:        #EF4444;
  --s-red-l:      rgba(239,68,68,0.10);

  /* Bordes */
  --s-border:     rgba(0,0,0,0.07);
  --s-border-md:  rgba(0,0,0,0.12);

  /* Sombras */
  --s-shadow-xs:  0 1px 3px rgba(0,0,0,0.06);
  --s-shadow-sm:  0 2px 10px rgba(0,0,0,0.07);
  --s-shadow-md:  0 4px 24px rgba(0,0,0,0.09);
  --s-shadow-lg:  0 12px 40px rgba(0,0,0,0.12);
  --s-shadow-green: 0 8px 28px rgba(0,201,128,0.28);

  /* Radios */
  --r-sm: 10px;
  --r-md: 16px;
  --r-lg: 24px;
  --r-xl: 32px;
  --r-pill: 999px;

  /* Tipografía */
  --s-font:    'Plus Jakarta Sans', system-ui, sans-serif;
  --s-body:    'Inter', system-ui, sans-serif;
  --s-mono:    'JetBrains Mono', 'Fira Code', monospace;
}

/* ── RESET SEED ── */
.seed-v2 {
  background: var(--s-bg);
  color: var(--s-ink);
  font-family: var(--s-body);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  width: 100%;
  overflow-x: hidden;
  /* Eliminado: scroll-snap y height estricto para un flujo libre continuo */
  scroll-behavior: smooth;
  position: relative;
}
.seed-v2 * { box-sizing: border-box; }

/* ── ORBES DE LUZ (BLOBS) ── */
.blob-container {
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  overflow: hidden;
  z-index: 0;
  pointer-events: none;
}
.blob {
  position: absolute;
  border-radius: 50%;
  filter: blur(120px);
  opacity: 0.6;
  z-index: 0;
  mix-blend-mode: multiply; /* Fusiona elegante sobre el fondo blanco */
}
.blob-cyan {
  background: rgba(59, 130, 246, 0.4); /* Azul cian vibrante */
}
.blob-emerald {
  background: rgba(0, 201, 128, 0.35); /* Verde vibrante */
}

/* ── SECCIONES ── */
.seed-section {
  min-height: 100vh; /* Permite crecer si hay mucho contenido */
  width: 100%;
  position: relative;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  background: transparent; /* Ahora el fondo general lo da .seed-v2, dejando ver los orbes */
  padding: 10vh 24px;
  z-index: 1; /* Por encima de los blobs */
}

/* Para la sección que sigue al Hero, asegurar que no corte el demo interactivo */
.seed-section.first-after-hero {
  margin-top: 5vh; /* Cambiado a positivo para que el demo respire y no se vea cortado */
  padding-top: 10vh;
  z-index: 2;
  border-top-left-radius: 40px;
  border-top-right-radius: 40px;
  background: rgba(255, 255, 255, 0.85); /* Glass base */
  backdrop-filter: blur(40px);
}

/* ── HERO (mantiene negro del HeroAnimation) ── */
.hero-dark-section {
  background: #000000 !important;
  height: auto !important;
  min-height: auto !important;
  overflow: visible !important;
  padding: 0 !important;
}

/* ════ TIPOGRAFÍA ════ */
.display-xl {
  font-family: var(--s-font);
  font-size: clamp(40px, 5.5vw, 72px);
  font-weight: 300;
  line-height: 1.05;
  letter-spacing: -0.03em;
  color: var(--s-ink);
}
.display-lg {
  font-family: var(--s-font);
  font-size: clamp(30px, 3.8vw, 52px);
  font-weight: 300;
  line-height: 1.08;
  letter-spacing: -0.025em;
  color: var(--s-ink);
}
.display-md {
  font-family: var(--s-font);
  font-size: clamp(22px, 2.5vw, 36px);
  font-weight: 400;
  line-height: 1.15;
  letter-spacing: -0.02em;
  color: var(--s-ink);
}
.text-accent {
  background: var(--s-grad);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}
.body-lg {
  font-family: var(--s-body);
  font-size: 17px;
  font-weight: 400;
  line-height: 1.75;
  color: var(--s-ink3);
}
.body-md {
  font-family: var(--s-body);
  font-size: 14px;
  font-weight: 400;
  line-height: 1.65;
  color: var(--s-ink3);
}
.label-sm {
  font-family: var(--s-body);
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--s-ink4);
}
.mono-data {
  font-family: var(--s-mono);
  font-size: 13px;
  font-weight: 500;
}

/* ════ EYEBROW LABEL ════ */
.eyebrow {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-family: var(--s-body);
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 0.09em;
  text-transform: uppercase;
  color: var(--s-green);
  margin-bottom: 16px;
}
.eyebrow::before {
  content: '';
  display: block;
  width: 18px;
  height: 2px;
  border-radius: 2px;
  background: var(--s-grad);
  flex-shrink: 0;
}

/* ════ CARDS (GLASSMORPHISM PREMIUM) ════ */
.card-premium {
  background: rgba(255, 255, 255, 0.65);
  backdrop-filter: blur(24px);
  -webkit-backdrop-filter: blur(24px);
  border: 1px solid rgba(255, 255, 255, 0.8);
  border-radius: var(--r-lg);
  box-shadow: 0 4px 24px rgba(0, 0, 0, 0.04), inset 0 1px 0 rgba(255, 255, 255, 0.8);
  transition: box-shadow 0.35s ease, transform 0.35s cubic-bezier(0.34, 1.56, 0.64, 1);
}
.card-premium:hover {
  box-shadow: 0 12px 40px rgba(0, 0, 0, 0.08), inset 0 1px 0 rgba(255, 255, 255, 1);
  transform: translateY(-4px) scale(1.01);
}

.card-alt {
  background: rgba(247, 248, 250, 0.5);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid rgba(255, 255, 255, 0.6);
  border-radius: var(--r-lg);
  box-shadow: var(--s-shadow-xs);
  transition: box-shadow 0.35s ease, transform 0.35s ease;
}
.card-alt:hover {
  box-shadow: var(--s-shadow-sm);
  transform: translateY(-2px);
  background: rgba(255, 255, 255, 0.7);
}

.card-green-tint {
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.8) 0%, rgba(240, 255, 249, 0.6) 100%);
  backdrop-filter: blur(24px);
  -webkit-backdrop-filter: blur(24px);
  border: 1px solid rgba(0, 201, 128, 0.25);
  border-radius: var(--r-lg);
  box-shadow: 0 8px 32px rgba(0, 201, 128, 0.08), inset 0 1px 0 rgba(255, 255, 255, 1);
}

.card-featured {
  background: rgba(255, 255, 255, 0.85);
  backdrop-filter: blur(32px);
  -webkit-backdrop-filter: blur(32px);
  border: 1.5px solid var(--s-green);
  border-radius: var(--r-lg);
  box-shadow: 0 12px 48px rgba(0, 201, 128, 0.25), inset 0 2px 0 rgba(255, 255, 255, 0.8);
  transform: scale(1.04);
  transition: box-shadow 0.35s ease, transform 0.35s ease;
}
.card-featured:hover {
  transform: scale(1.06);
  box-shadow: 0 20px 60px rgba(0, 201, 128, 0.35), inset 0 2px 0 rgba(255, 255, 255, 1);
}

/* ════ BADGES / CHIPS / PILLS ════ */
.chip {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 5px 13px;
  border-radius: var(--r-pill);
  font-family: var(--s-body);
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 0.04em;
}
.chip-green {
  background: var(--s-green-l);
  color: var(--s-green-d);
  border: 1px solid rgba(0,201,128,0.20);
}
.chip-blue {
  background: var(--s-blue-l);
  color: var(--s-blue);
  border: 1px solid rgba(59,130,246,0.20);
}
.chip-red {
  background: var(--s-red-l);
  color: var(--s-red);
  border: 1px solid rgba(239,68,68,0.20);
}
.chip-ink {
  background: rgba(9,9,15,0.06);
  color: var(--s-ink2);
  border: 1px solid rgba(9,9,15,0.10);
}

/* ════ BOTONES ════ */
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  border-radius: var(--r-pill);
  font-family: var(--s-font);
  font-weight: 600;
  font-size: 14px;
  padding: 13px 28px;
  cursor: pointer;
  border: none;
  outline: none;
  transition: all 0.22s cubic-bezier(0.34,1.56,0.64,1);
  text-decoration: none;
  white-space: nowrap;
}
.btn-primary {
  background: var(--s-grad);
  color: #fff;
  box-shadow: var(--s-shadow-green);
}
.btn-primary:hover {
  transform: scale(1.04) translateY(-2px);
  box-shadow: 0 12px 36px rgba(0,201,128,0.38);
}
.btn-ghost {
  background: transparent;
  color: var(--s-ink2);
  border: 1.5px solid var(--s-border-md);
}
.btn-ghost:hover {
  background: var(--s-bg-alt);
  border-color: var(--s-ink4);
}
.btn-green-outline {
  background: transparent;
  color: var(--s-green-d);
  border: 1.5px solid rgba(0,201,128,0.35);
}
.btn-green-outline:hover {
  background: var(--s-green-l);
  border-color: var(--s-green);
}
.btn-sm { padding: 9px 20px; font-size: 13px; }
.btn-lg { padding: 16px 36px; font-size: 15px; }

/* ════ STAT / NÚMERO GRANDE ════ */
.stat-number {
  font-family: var(--s-font);
  font-size: clamp(48px, 7vw, 96px);
  font-weight: 800;
  line-height: 1;
  letter-spacing: -0.04em;
  color: var(--s-ink);
}
.stat-number.green { color: var(--s-green); }
.stat-label {
  font-family: var(--s-body);
  font-size: 13px;
  font-weight: 500;
  color: var(--s-ink3);
  margin-top: 4px;
}

/* ════ ICON CONTAINER ════ */
.icon-wrap {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 48px;
  height: 48px;
  border-radius: var(--r-md);
  background: var(--s-green-l);
  color: var(--s-green);
  flex-shrink: 0;
}
.icon-wrap-lg {
  width: 64px;
  height: 64px;
  border-radius: var(--r-lg);
}
.icon-wrap-blue {
  background: var(--s-blue-l);
  color: var(--s-blue);
}
.icon-wrap-red {
  background: var(--s-red-l);
  color: var(--s-red);
}
.icon-wrap-ink {
  background: rgba(9,9,15,0.06);
  color: var(--s-ink2);
}
.icon-wrap-circle {
  border-radius: 50%;
}

/* ════ DIVIDER ════ */
.divider {
  width: 100%;
  height: 1px;
  background: var(--s-border);
}

/* ════ QUOTE BLOCK ════ */
.quote-block {
  background: var(--s-bg-alt);
  border-left: 3px solid var(--s-green);
  border-radius: 0 var(--r-md) var(--r-md) 0;
  padding: 20px 24px;
  font-family: var(--s-body);
  font-size: 15px;
  font-style: italic;
  font-weight: 400;
  line-height: 1.7;
  color: var(--s-ink2);
}

/* ════ DATA ROW ════ */
.data-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 14px 0;
  border-bottom: 1px solid var(--s-border);
  font-size: 13.5px;
}
.data-row:last-child { border-bottom: none; }

/* ════ STEP CONNECTOR ════ */
.step-line {
  position: absolute;
  top: 24px;
  left: calc(50% + 28px);
  right: calc(-50% + 28px);
  height: 2px;
  background: linear-gradient(90deg, var(--s-green-l), rgba(0,201,128,0.25));
  z-index: 0;
}

/* ════ IMAGE PLACEHOLDER ════ */
.img-placeholder {
  background: var(--s-bg-alt);
  border: 2px dashed var(--s-border-md);
  border-radius: var(--r-lg);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 10px;
  color: var(--s-ink4);
  font-family: var(--s-mono);
  font-size: 11px;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  text-align: center;
}
.img-placeholder-sm { height: 120px; }
.img-placeholder-md { height: 200px; }
.img-placeholder-lg { height: 300px; }

/* ════ SCROLL FADE-IN ════ */
.s-fade {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.55s ease, transform 0.55s ease;
}
.s-fade.s-visible {
  opacity: 1;
  transform: translateY(0);
}
.s-fade-delay-1 { transition-delay: 0.08s; }
.s-fade-delay-2 { transition-delay: 0.16s; }
.s-fade-delay-3 { transition-delay: 0.24s; }
.s-fade-delay-4 { transition-delay: 0.32s; }
.s-fade-delay-5 { transition-delay: 0.40s; }

/* ════ GOOGLE ECOSYSTEM COLORS ════ */
.g-drive   { --g-color: #1A73E8; }
.g-cal     { --g-color: #EA4335; }
.g-sheets  { --g-color: #0F9D58; }
.g-docs    { --g-color: #4285F4; }
.g-forms   { --g-color: #7248B9; }
.g-oauth   { --g-color: #F9AB00; }

/* ════ BENTO GRID ════ */
.bento-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  grid-template-rows: auto auto;
  gap: 16px;
  width: 100%;
}
.bento-hero   { grid-column: span 2; }
.bento-tall   { grid-row: span 2; }

/* ════ PRICING ════ */
.price-tag {
  font-family: var(--s-font);
  font-size: 54px;
  font-weight: 800;
  line-height: 1;
  letter-spacing: -0.04em;
  color: var(--s-ink);
}
.price-currency {
  font-size: 22px;
  font-weight: 600;
  vertical-align: top;
  position: relative;
  top: 10px;
  color: var(--s-ink3);
}
.price-period {
  font-size: 13px;
  color: var(--s-ink4);
  font-weight: 400;
  font-family: var(--s-body);
}

/* ════ GUARANTEE ROW ════ */
.guarantee-row {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  justify-content: center;
  gap: 8px 28px;
  font-family: var(--s-body);
  font-size: 12px;
  font-weight: 500;
  color: var(--s-ink4);
}
.guarantee-item {
  display: flex;
  align-items: center;
  gap: 6px;
}

/* ════ LASER BORDER ANIMATION ════ */
@keyframes laserBorder {
  0%   { background-position: 0% 0%; }
  100% { background-position: 300% 0%; }
}
.laser-border {
  position: relative;
}
.laser-border::before {
  content: '';
  position: absolute;
  inset: -1.5px;
  border-radius: inherit;
  padding: 1.5px;
  background: linear-gradient(90deg,
    transparent 0%,
    var(--s-green) 25%,
    var(--s-green-d) 50%,
    transparent 75%
  );
  background-size: 300% 100%;
  animation: laserBorder 2.5s linear infinite;
  -webkit-mask: linear-gradient(#fff 0 0) content-box,linear-gradient(#fff 0 0);
  -webkit-mask-composite: xor;
  mask-composite: exclude;
}

/* ════ COUNTER ANIMATION ════ */
@keyframes countDown {
  from { opacity: 0; transform: scale(0.8); }
  to   { opacity: 1; transform: scale(1); }
}

/* ════ FLOAT ANIMATION ════ */
@keyframes floatY {
  0%, 100% { transform: translateY(0); }
  50%       { transform: translateY(-10px); }
}
.float-anim { animation: floatY 4s ease-in-out infinite; }

/* ════ KEYFRAMES SCROLL ════ */
@keyframes dashFlow { to { stroke-dashoffset: -100; } }
@keyframes glowPulse {
  0%, 100% { box-shadow: var(--s-shadow-green); }
  50%       { box-shadow: 0 16px 48px rgba(0,201,128,0.40); }
}

/* ════ HERO DARK (se mantiene igual del HeroAnimation) ════ */
.hero-tooth-wrap {
  position: absolute; top: 50%; left: 50%;
  transform: translate(-50%, -50%);
  z-index: 3; pointer-events: none;
}
@keyframes toothGlowPulse {
  0%,100% { filter: drop-shadow(0 0 12px rgba(255,255,255,0.5)) drop-shadow(0 0 40px rgba(16,185,129,0.25)); }
  50%      { filter: drop-shadow(0 0 28px rgba(255,255,255,0.95)) drop-shadow(0 0 80px rgba(16,185,129,0.55)); }
}
.tooth-glow-anim { animation: toothGlowPulse 2.6s ease-in-out infinite; }
.hero-hand-human {
  position: absolute; width: clamp(300px,45vw,680px);
  pointer-events: none; z-index: 2; object-fit: contain;
  top: 50%; left: -2%; mix-blend-mode: screen;
}
.hero-hand-robot {
  position: absolute; width: clamp(300px,45vw,680px);
  pointer-events: none; z-index: 2; object-fit: contain;
  top: 50%; right: -2%; mix-blend-mode: screen;
}
.hero-dark-content {
  position: absolute; bottom: 0; left: 0; right: 0;
  z-index: 10; padding: 0 6% 5vh 6%;
  display: flex; flex-direction: column;
  align-items: flex-start; pointer-events: auto;
}
.hero-dark-nav {
  position: absolute; top: 0; left: 0; right: 0;
  z-index: 20; display: flex; align-items: center;
  justify-content: space-between; padding: 0 6%; height: 68px;
}
.hero-title-mono {
  font-family: 'DM Mono','Fira Code',monospace !important;
  font-weight: 500; font-size: clamp(26px,3.8vw,68px);
  line-height: 1.12; letter-spacing: 0.04em;
  text-transform: uppercase; color: #ffffff;
}
.hero-subtitle-mono {
  font-family: 'DM Mono','Fira Code',monospace !important;
  font-size: clamp(12px,1.3vw,16px); font-weight: 400;
  color: rgba(255,255,255,0.50); line-height: 1.75; letter-spacing: 0.02em;
}
.glass-chip-dark {
  display: inline-flex; align-items: center; gap: 8px;
  background: rgba(255,255,255,0.07); border: 1px solid rgba(255,255,255,0.16);
  backdrop-filter: blur(20px); border-radius: 999px; padding: 8px 18px;
  font-family: 'DM Mono',monospace; font-size: 9.5px; font-weight: 500;
  letter-spacing: 0.11em; text-transform: uppercase;
  color: rgba(255,255,255,0.75); transition: all 0.22s ease;
}
.glass-chip-dark:hover {
  background: rgba(255,255,255,0.14); border-color: rgba(16,185,129,0.50);
  color: #fff; transform: translateY(-2px);
}
.btn-dark-primary {
  display: inline-flex; align-items: center; gap: 10px;
  background: #10b981; color: #000;
  font-family: 'DM Mono',monospace; font-size: 12px;
  font-weight: 500; letter-spacing: 0.08em; text-transform: uppercase;
  border-radius: 999px; padding: 13px 30px; border: none; cursor: pointer;
  transition: all 0.22s cubic-bezier(0.34,1.56,0.64,1);
  box-shadow: 0 0 24px rgba(16,185,129,0.40);
}
.btn-dark-primary:hover {
  background: #34d399; box-shadow: 0 0 42px rgba(16,185,129,0.65);
  transform: scale(1.04) translateY(-2px);
}
.btn-dark-ghost {
  display: inline-flex; align-items: center; gap: 10px;
  background: rgba(255,255,255,0.06); color: rgba(255,255,255,0.70);
  font-family: 'DM Mono',monospace; font-size: 12px;
  font-weight: 400; letter-spacing: 0.08em; text-transform: uppercase;
  border-radius: 999px; padding: 13px 30px;
  border: 1.5px solid rgba(255,255,255,0.16); cursor: pointer;
  backdrop-filter: blur(16px); transition: all 0.22s ease;
}
.btn-dark-ghost:hover {
  background: rgba(255,255,255,0.12); border-color: rgba(255,255,255,0.35); color: #fff;
}
.logo-dark {
  font-family: 'DM Mono',monospace !important; font-weight: 500; font-size: 16px;
  letter-spacing: 0.12em; text-transform: uppercase; color: #ffffff;
  display: flex; align-items: center; gap: 8px;
}
.logo-seed-badge-dark {
  font-family: 'DM Mono',monospace; font-size: 9px; font-weight: 500;
  letter-spacing: 0.20em; color: #10b981;
  border: 1px solid rgba(16,185,129,0.50); border-radius: 4px;
  padding: 3px 8px; text-transform: uppercase;
}
.nav-link-dark {
  font-family: 'DM Mono',monospace; font-size: 10.5px; font-weight: 400;
  letter-spacing: 0.10em; text-transform: uppercase;
  color: rgba(255,255,255,0.40); text-decoration: none;
  padding: 6px 12px; border-radius: 999px; transition: all 0.18s ease;
}
.nav-link-dark:hover { color: rgba(255,255,255,0.85); background: rgba(255,255,255,0.06); }

/* ════ RESPONSIVE ════ */
@media (max-width: 1024px) {
  .bento-grid { grid-template-columns: repeat(2, 1fr); }
  .bento-hero { grid-column: span 2; }
  .bento-tall { grid-row: span 1; }
}
@media (max-width: 768px) {
  .seed-section { padding: 0 16px; }
  .bento-grid { grid-template-columns: 1fr; }
  .bento-hero { grid-column: span 1; }
  .hero-hand-human { width: clamp(220px,68vw,460px); left: -8%; }
  .hero-hand-robot  { width: clamp(220px,68vw,460px); right: -8%; }
  .hero-dark-content { padding: 0 5% 9vh 5%; }
  .card-featured { transform: scale(1); }
}
@media (max-width: 480px) {
  .hero-hand-human { width: 80vw; opacity: 0.70; left: -10%; }
  .hero-hand-robot  { width: 80vw; opacity: 0.70; right: -10%; }
}

/* ════ NUEVAS SECCIONES DEL PLAN MAESTRO ════ */
/* Calculadora (Paso 2) */
.calc-card {
  background: rgba(255, 255, 255, 0.65);
  backdrop-filter: blur(30px);
  -webkit-backdrop-filter: blur(30px);
  border: 1px solid rgba(0, 0, 0, 0.06);
  border-radius: var(--r-xl);
  padding: 40px;
  box-shadow: 0 10px 30px rgba(0,0,0,0.02);
  margin-top: 32px;
}
.calc-title {
  font-family: var(--s-font);
  font-weight: 800;
  color: var(--s-ink);
  letter-spacing: -0.02em;
}
.calc-slider-group {
  margin-bottom: 28px;
}
.calc-slider-label {
  display: flex;
  justify-content: space-between;
  font-family: var(--s-body);
  font-size: 13px;
  font-weight: 600;
  color: var(--s-ink2);
  margin-bottom: 10px;
}
.calc-slider-value {
  font-family: var(--s-mono);
  color: var(--s-green-d);
  font-weight: 700;
}
.calc-input-range {
  width: 100%;
  height: 6px;
  background: var(--s-bg-alt);
  border-radius: var(--r-pill);
  outline: none;
  -webkit-appearance: none;
  accent-color: var(--s-green);
  border: 1px solid rgba(0, 0, 0, 0.05);
}
.calc-input-range::-webkit-slider-thumb {
  -webkit-appearance: none;
  width: 18px;
  height: 18px;
  border-radius: 50%;
  background: var(--s-green);
  border: 2px solid #fff;
  box-shadow: 0 2px 6px rgba(0,0,0,0.15);
  cursor: pointer;
  transition: transform 0.1s;
}
.calc-input-range::-webkit-slider-thumb:hover {
  transform: scale(1.15);
}
.calc-result-box {
  background: rgba(239, 68, 68, 0.03);
  border: 1px solid rgba(239, 68, 68, 0.12);
  border-radius: var(--r-lg);
  padding: 30px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-align: center;
}
.calc-loss-value {
  font-family: var(--s-font);
  font-size: clamp(38px, 4vw, 56px);
  font-weight: 800;
  color: var(--s-red);
  line-height: 1;
  letter-spacing: -0.03em;
}
.calc-loss-desc {
  font-family: var(--s-body);
  font-size: 13px;
  color: var(--s-ink3);
  margin-top: 10px;
  line-height: 1.6;
}

/* Tarjetas Premium Sólidas (Sin efecto cristal/glassmorphismo para mejorar legibilidad y contraste) */
.glass-card-apple {
  position: relative;
  overflow: hidden;
  background: #fcfcfd !important; /* Fondo sólido muy limpio */
  border: 1px solid rgba(9, 9, 15, 0.06) !important;
  box-shadow: 0 8px 30px rgba(0, 0, 0, 0.02) !important;
  
  /* Renderizado de texto súper nítido sin forzar capas GPU rasterizadas */
  -webkit-font-smoothing: subpixel-antialiased;
  -moz-osx-font-smoothing: auto;
  transform: translateY(0) !important;
  
  transition: transform 0.4s cubic-bezier(0.25, 1, 0.5, 1) !important, 
              box-shadow 0.4s cubic-bezier(0.25, 1, 0.5, 1) !important, 
              border-color 0.4s ease !important;
}
.glass-card-apple::after {
  display: none;
}
.glass-card-apple:hover {
  transform: translateY(-6px) !important;
  box-shadow: 0 20px 45px rgba(9, 9, 15, 0.04) !important;
  border-color: rgba(9, 9, 15, 0.12) !important;
}

.glass-card-apple-red {
  position: relative;
  overflow: hidden;
  background: #fff8f8 !important; /* Fondo rojo sólido muy suave */
  border: 1px solid rgba(239, 68, 68, 0.12) !important;
  box-shadow: 0 8px 30px rgba(239, 68, 68, 0.01) !important;
  
  /* Renderizado de texto súper nítido sin forzar capas GPU rasterizadas */
  -webkit-font-smoothing: subpixel-antialiased;
  -moz-osx-font-smoothing: auto;
  transform: translateY(0) !important;
  
  transition: transform 0.4s cubic-bezier(0.25, 1, 0.5, 1) !important, 
              box-shadow 0.4s cubic-bezier(0.25, 1, 0.5, 1) !important, 
              border-color 0.4s ease !important;
}
.glass-card-apple-red::after {
  display: none;
}
.glass-card-apple-red:hover {
  transform: translateY(-6px) !important;
  box-shadow: 0 20px 45px rgba(239, 68, 68, 0.05) !important;
  border-color: rgba(239, 68, 68, 0.22) !important;
}

/* Especialidades (Paso 3) */
.spec-grid-landing {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 16px;
  margin-top: 40px;
}
.spec-card-landing {
  background: rgba(255, 255, 255, 0.65);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(0, 0, 0, 0.06);
  border-radius: var(--r-md);
  padding: 24px;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}
.spec-card-landing:hover {
  transform: translateY(-4px);
  box-shadow: var(--s-shadow-sm);
  background: rgba(255, 255, 255, 0.85);
}
.spec-icon-box {
  font-size: 28px;
  margin-bottom: 12px;
}
.spec-title-landing {
  font-family: var(--s-font);
  font-size: 16px;
  font-weight: 700;
  color: var(--s-ink);
  margin-bottom: 4px;
}
.spec-desc-landing {
  font-family: var(--s-body);
  font-size: 12px;
  color: var(--s-ink3);
  line-height: 1.6;
}

/* Secciones CORE (Paso 3) */
.core-grid-landing {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12px;
  margin-top: 40px;
}
.core-card-landing {
  background: rgba(255, 255, 255, 0.5);
  border: 1px solid rgba(0, 0, 0, 0.05);
  border-radius: var(--r-md);
  padding: 18px;
  display: flex;
  gap: 14px;
  transition: all 0.25s ease;
}
.core-card-landing:hover {
  background: rgba(255, 255, 255, 0.8);
  border-color: rgba(0, 201, 128, 0.2);
}
.core-num-landing {
  font-family: var(--s-mono);
  font-size: 13px;
  font-weight: 700;
  color: var(--s-green-d);
  background: var(--s-green-l);
  width: 28px;
  height: 28px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
.core-content-landing {
  flex: 1;
}
.core-title-landing {
  font-family: var(--s-font);
  font-size: 13.5px;
  font-weight: 700;
  color: var(--s-ink);
  margin-bottom: 4px;
}
.core-desc-landing {
  font-family: var(--s-body);
  font-size: 11.5px;
  color: var(--s-ink3);
  line-height: 1.5;
}

/* FAQ Acordeón (Paso 10) */
.faq-accordion {
  max-width: 800px;
  width: 100%;
  margin: 40px auto 0;
  display: flex;
  flex-direction: column;
  gap: 12px;
}
.faq-item-landing {
  background: rgba(255, 255, 255, 0.6);
  border: 1px solid rgba(0, 0, 0, 0.06);
  border-radius: var(--r-md);
  overflow: hidden;
  transition: all 0.25s ease;
}
.faq-item-landing.active {
  background: rgba(255, 255, 255, 0.85);
  border-color: rgba(0, 201, 128, 0.25);
  box-shadow: var(--s-shadow-sm);
}
.faq-trigger-landing {
  width: 100%;
  padding: 20px 24px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: transparent;
  border: none;
  cursor: pointer;
  text-align: left;
  outline: none;
}
.faq-question-landing {
  font-family: var(--s-font);
  font-size: 15px;
  font-weight: 700;
  color: var(--s-ink);
}
.faq-icon-landing {
  color: var(--s-ink3);
  transition: transform 0.2s ease;
}
.faq-item-landing.active .faq-icon-landing {
  transform: rotate(180deg);
  color: var(--s-green);
}
.faq-content-landing {
  max-height: 0;
  overflow: hidden;
  transition: max-height 0.3s ease, padding 0.3s ease;
  padding: 0 24px;
}
.faq-item-landing.active .faq-content-landing {
  max-height: 250px;
  padding: 0 24px 20px;
}
.faq-answer-landing {
  font-family: var(--s-body);
  font-size: 13.5px;
  color: var(--s-ink3);
  line-height: 1.65;
}

/* Responsivo para las nuevas secciones */
@media (max-width: 1024px) {
  .spec-grid-landing { grid-template-columns: repeat(2, 1fr); }
  .core-grid-landing { grid-template-columns: repeat(2, 1fr); }
}
@media (max-width: 768px) {
  .calc-card { padding: 24px; }
  .calc-grid-layout { grid-template-columns: 1fr !important; gap: 24px; }
  .spec-grid-landing { grid-template-columns: 1fr; }
  .core-grid-landing { grid-template-columns: 1fr; }
}

